# Changelog de Bloques

Historial de versiones de `index.html`. Extraido del propio codigo el 17-jul-2026 
para aligerar el archivo (estos comentarios no aportaban nada al usuario final ni al motor,
solo pesaban en cada lectura). Orden: mas reciente primero.

> Nota: v3.00–v3.22 (vista unificada Screener->Earnings, historico reconstruido por API,
> limpieza de Ajustes) siguen documentadas en la memoria del proyecto, no aqui todavia.
> A partir de v3.23 se vuelven a registrar aqui — queda pendiente el backfill de v3.00–v3.22.

v3.26 (Victor: "quítame de ajustes la selección de api de market data porque ya no hace falta" y "en cada ticker del screener pon que se vea el cuadro de POP al open... junto con los de market cap y demás"): DOS COSAS. (1) RETIRADA la key de Alpha Vantage de Ajustes → API keys ("Fechas de earnings (Alpha Vantage)", cuyo subtítulo decía literalmente "el Screener la necesita para reconstruir el POP" — texto ya obsoleto desde que el servidor (VPS + moomoo) hace esa reconstrucción; era el único candidato de "api de market data" que quedaba visible, dado que Market Data (marketdata.app) ya se había retirado en v3.23). Eliminados avKey/avModalOpen/avInput + openAvModal/saveAvKey + su modal + su carga de localStorage + KEY_AVANTAGE fuera de BACKUP_KEYS y del export/import/restore del backup. fetchEarningsHistoryAV y reconstructHistory (esta última ya sin llamadores desde v3.23) se dejan como código muerto inofensivo — mismo criterio que con Market Data. (2) POP OPEN VISIBLE EN LA CABECERA de cada tarjeta del Screener, sin desplegar: nuevo mini-tile "POP" (helpers popOf/popBg reutilizados del filtro v3.24), primero de los cuatro junto a cap/IV/opc-día — verde ≥75%/ámbar ≥50%/rojo <75%, "—" gris cuando el ticker no tiene histórico. Antes solo se veía al tocar la tarjeta (statsOf, coste de leer localStorage por tarjeta abierta); ahora usa la misma lectura única de erHistAll ya hecha para el filtro, sin coste extra. Verificado babel.

v3.25 (Victor, con captura de GOOG: "no está cogiendo el EM próximo, ni siquiera en GOOG hay dato — sin eso no se puede montar el IC"): NO es un bug del cliente. El screener.json del servidor (enriquecer.py en el VPS, FASE B de v3.20) solo reconstruye earnings PASADOS de moomoo (is_current=False) — el trimestre en curso (el que hace falta para el IC) nunca se incluyó como evento "pendiente", así que erStats().nextEm sale siempre NaN para TODOS los tickers, no solo GOOG. Ya estaba anotado como pendiente en la memoria del proyecto tras la FASE B (17 jul), no se había vuelto a tocar. Arreglo aplicado en la app (aclaración, no solución de raíz — esa vive en el VPS): bajo "EM próximo" con "—", nueva nota ámbar "El servidor aún no manda el EM del trimestre en curso — para montar el IC, léelo en Earnings con la tabla de moomoo o metiendo spot+EM a mano", para que no parezca un fallo silencioso. FIX DE RAÍZ (pendiente, en el VPS, fuera del alcance de este chat): enriquecer.py necesita también leer la fila is_current=True de price_history (da predict_vola_ratio_newest/highest del trimestre en curso) y añadirla a events como { periodo: "próx", date, hour, em, open: null, close: null, high: null, low: null, ivCrush: null } — mismo formato que erStats ya espera para "pending" (open==null → EM próximo). Verificado babel.

v3.24 (Victor: "se sigue sin poder filtrar por pop en el screener" — con captura): la v3.23 metió el filtro POP en la tabla de Earnings por error; lo que quería era el SCREENER. El Screener (lee screener.json del servidor) tenía filtros de Cap/Liquidez/Sesión/día pero no de POP. Nuevo selector "POP ≥" (todas/≥50/≥60/≥70/≥75%) junto a los demás, persistido en KEY_SCRMINPOP (fracción 0–1, la const ya existía de la v2.01). Filtra por la POP open del histórico del servidor (helper popOf con una sola lectura de erHist por render; los tickers sin histórico no puntúan y salen cuando hay umbral). El contador "N de M pasan los filtros" añade el aviso "POP open ≥ X% (los tickers sin histórico no puntúan)". Verificado babel.

v3.23 (Victor: "que se pueda filtrar por pop" y "quita market data api de ajustes"): DOS COSAS. (1) FILTRO POR POP en el "Resumen por ticker" de la herramienta Earnings — fila de chips Todas/≥50%/≥60%/≥70% sobre la columna "Dentro EM" (r.within = popOpen), que filtra tras ordenar, con contador "N de M" y aviso "Ningún ticker con POP ≥ X%" cuando ninguno cumple; estado erSumMinPop efímero (sin backup). La tabla ya era ordenable, ahora también filtrable. (2) RETIRADA la API de Market Data (marketdata.app): la fila de Ajustes ya no existía (sustituida por Alpha Vantage; solo quedaban el comentario y un modal huérfano al que no llegaba ningún botón). Eliminado el resto de la integración — getLiq() + estado liq/liqBusy, botón "Comprobar liquidez" y su panel de resultado (el del rótulo "Market Data (…)"), el badge de liquidez de las tarjetas de earnings, la columna "Liq" del Resumen por ticker (minWidth 540→470), el modal de la key + estado mdataKey/mdataModalOpen/mdataInput + open/saveMdataKey + la carga de localStorage, KEY_MDATA fuera de BACKUP_KEYS y del export/import/restore del backup, y los textos ya obsoletos del panel "Criterios de liquidez mínima" (referencias al chequeo Market Data/CBOE). Se dejaron como código muerto inofensivo fetchMdExpirations/fetchMdChain/fetchStockCandlesMD/reconstructHistory (sin llamadores) y liqVerdict (sin usos) + la const KEY_MDATA, para no desenredar helpers compartidos (liqStats, pickErExpiry, parseStrikeTok, emFromChain) y arriesgar un crash. El Screener no se tocó — sigue leyendo screener.json del servidor. Verificado babel.

v2.15 (Victor: "en la posición de cash, que el ticker automático sea CASH y no se pueda cambiar"): al entrar al paso 3 con Cash el asistente fija wizForm.tkr="CASH"; el campo Ticker se pinta como un recuadro fijo "CASH" no editable (en vez del WizInput); y buildPos fuerza tkr="CASH" para cualquier posición de cash (a prueba de rutas alternativas). Verificado babel.

v2.14 (Victor, cinco frentes): (1) el "Resumen por ticker" de Earnings gana columna "Próx" con la fecha del próximo earnings y pre/post (calDate/calHour del histórico). (2) En el "Resumen del barrido" del Screener, pulsar un ticker ya NO salta directo a Earnings: sale un aviso de confirmación ("Pasar a Earnings · TKR") con Cancelar / Ir a Earnings, para no cambiar de pestaña sin querer al ordenar o leer la tabla; al confirmar, goEarnings sigue mostrando el banner de tarjeta auto-creada. (3) COMISIÓN DE COMPRA por lote de acciones: el lote gana un campo fee (comisión de compra) que sube el BEP repartiéndose entre las acciones del lote — nuevo helper lotUnit (coste efectivo por acción = precio + fee/qty), cableado en lotsBep, applyLots, fifoRealized y fifoSell (al partir un lote, la comisión restante se reparte proporcional); el editor de lotes muestra "Comisión de compra" y el BEP del lote/posición ya la incluye. La comisión de VENTA ya existía. (4) CASH en el asistente: al elegir Cash solo se pide "Importe (USD)" (se guarda en mktValue), sin acciones ni BEP; el resto de la app ya trataba el cash por mktValue. (5) TARJETA-RESUMEN AL CERRAR cualquier posición: al confirmar el cierre (closePos) sale un modal PosSummaryCard con P&L realizado, ROI, Annualized ROI, Riesgo/Invertido, BEP, DTE y DIT — las mismas cifras de Histórico pero autónomas (nuevo realizedOf(p), réplica byte a byte del ledger primaEvents/mtmEvents para UNA posición: mtm para acciones/long call/credit spreads, prima acumulada para short put/CC/PMCC); la tarjeta se cierra con "Hecho" y te quedas en el MISMO menú (no lleva a Histórico). Verificado con node (realizedOf vs ledger en short put rolado, covered call, credit spread, long call, PMCC y acciones con lotes+comisión) + babel.

v2.13 (Victor: cuatro cosas — "¿no tenemos IV crush? ¿es posible obtenerlo?"; "al pinchar en el listado y pasar a Earnings, un aviso para que el usuario lo sepa"; "el histórico de POP de NFLX no está bien, debería dar 50% y da 75%"; "en el resumen por ticker solo salen 4 eventos, deberían ser 12"; "¿qué es Ø edge?"): las preguntas 3 y 4 tenían la MISMA causa raíz. fetchMdChain(date) — la cadena histórica de opciones — usaba `fetch` PLANO sin reintento (a diferencia de fetchMdExpirations, que sí tenía fetchRetry desde v2.06), y el bucle de reconstructHistory encadenaba 2-3 llamadas históricas por earnings SIN PAUSA entre eventos: bajo rate-limit, la llamada fallaba en silencio, el evento se guardaba con em:NaN, y erStats lo EXCLUÍA de M sin avisar — de ahí que NFLX mostrara solo 4 eventos en vez de 12, y con 4 eventos y 1 rotura el POP crudo sale 75% (1−1/4) en vez del ~50% que darían 12 con ~6 roturas. FIX: (1) fetchRetry también en la rama histórica de fetchMdChain. (2) pausa de 150ms entre cada earnings del bucle de reconstrucción. (3) nuevo diagnóstico nReq/nEmOk (earnings pedidos vs earnings que sí consiguieron EM) devuelto por reconstructHistory y mostrado en la sonda: "Histórico incompleto: N de M earnings con dato de opciones" — nunca en silencio. Sobre IV CRUSH (pregunta 1): SÍ es posible — Market Data devuelve el campo `iv` por contrato en la cadena (confirmado en su documentación); nuevo emFromChainX devuelve también el strike ATM usado, y reconstructHistory hace una SEGUNDA llamada histórica a la MISMA cadena y vencimiento el día en que se resuelve el gap (después del earnings) para leer el IV del MISMO strike y calcular ivCrush = (IV antes − IV después) × 100, en puntos porcentuales (mismo formato "en %" que usa el lector de tablas de moomoo). Coste: +1 llamada histórica por evento pasado (~12 más por ticker; la key de Market Data Trader no tiene problema de cupo). Sobre el AVISO del salto automático (pregunta 2): goEarnings marca el patch con auto:true cuando trae eventos reconstruidos; la herramienta Earnings muestra un banner morado "Tarjeta de TKR creada automáticamente desde el Screener (Market Data) — POP/EM/edge vienen de la reconstrucción, no de una captura" 7s al aterrizar. Sobre Ø EDGE (pregunta 4): es ØEM − Ø|open| — la media del expected move menos la media del movimiento real absoluto; positivo significa que el mercado esperaba más movimiento del que hubo (colchón a favor del vendedor de la prima), negativo que el movimiento real superó de media al EM implícito. Añadido tooltip (title) en la cabecera "Ø edge" de ambas tablas-resumen (Screener y Earnings) con esta misma explicación. Verificado node (emFromChainX devuelve strike correcto; IV crush 65%→38% da 27 pts) + babel.

v2.12 (Victor: "cuando se descargue y filtre, que los resultados se queden grabados en el Screener sin re-gastar llamadas; al seleccionar una posición que se la lleve a Earnings Y CREE LA TARJETA; la sonda individual que se añada a una lista con opción de abrir en Earnings; y en Earnings que tome el dato de todo desde esa nueva información"): la reconstrucción del Screener ahora ALIMENTA las tarjetas de Earnings. (1) El barrido guarda los eventos reconstruidos por nombre (rec.recEvents = rc.events) en el payload de KEY_SCREEN, así crear la tarjeta al seleccionar NO vuelve a llamar a la API. (2) Nuevo helper goEarnings(sym, events, spot, em, ic): guarda los eventos en hist[sym].mdEvents (+ calDate/calHour del próximo earnings) SIN tocar los de moomoo (hist[sym].events) y salta a Earnings; los 3 puntos de salto (sonda, tarjeta del barrido, fila del resumen) lo usan. (3) Nuevo helper de módulo erEvents(h) = mdEvents si los hay, si no events; la herramienta Earnings lo usa en applyHist, la tabla-resumen, el carrusel y el comparador de anchos → POP/EM/edge salen de la reconstrucción en cuanto existe, y el histórico de moomoo se conserva de contraste en el Screener. (4) SONDA individual: cada búsqueda se añade a una lista persistente (KEY_SCRPROBES, dedup por ticker, tope 30) que se pinta bajo el recuadro de la sonda con POP/ØEM/fecha + botón "→ Earnings" (crea la tarjeta) y ✕. Como el switch de sub-herramientas es un ternario, al saltar EarningsToolContent remonta y relee el histórico con los mdEvents recién guardados. Verificado node (erEvents prefiere md sin borrar moomoo; goEarnings preserva moomoo + guarda md + fija fecha/sesión) + babel. Los mdEvents viajan en el backup (van dentro de KEY_ERHIST).

v2.11 FIX de datos (Victor, captura: "los datos no son correctos, pone POP 100 en todos" — con Ø edge +13/+25 y Ø EM 22-31%): la reconstrucción ya funcionaba (Alpha Vantage puesta) pero el EM histórico salía ~4-5× inflado, y como todos los moves reales (~5%) caían dentro de ese EM gigante → POP 100% y Ø edge ØEM−Ø|open| ≈ +17 falsos en todos. CAUSA: pickErExpiry elegía el vencimiento respecto a HOY, no respecto a la fecha del earnings. En la reconstrucción, calDate es una fecha PASADA (<hoy), así que `calDate>=today` fallaba y ref caía a hoy → se colaba el primer vencimiento posterior a hoy, un LEAP a 1-2 años que YA cotizaba en la fecha histórica → straddle ~20-30% del spot. Arreglado: ref = calDate || today (la fecha del earnings, pasada o futura); strict=amc siempre; y la reconstrucción pasa ev.date (fecha del earnings) en vez del gap day. Verificado node: histórico 2024 elige el semanal correcto (no el LEAP), y el caso live (futuro) no cambia. Los moves de las velas ya estaban bien; esto solo corrige el straddle.

v2.10 (Victor, captura: "sigue igual todo" — la sonda de NFLX y el barrido siguen dando "Finnhub sin earnings pasados"): el cableado de Alpha Vantage (v2.08) está bien; el error dice "Finnhub" porque avKey estaba VACÍO en runtime → Victor aún NO ha puesto la key de Alpha Vantage en Ajustes, así que la reconstrucción cae al fallback de Finnhub (que en gratis no da pasados) y el mensaje confunde (parece fallo de datos, no de config). Ahora runProbe y runLiquidez EXIGEN la key de Alpha Vantage antes de reconstruir: si falta, mensaje claro y accionable "Falta la key de Alpha Vantage — Ajustes → Datos → Fechas de earnings" en vez del confuso "Finnhub sin earnings pasados". Sin cambios de lógica de reconstrucción.

v2.09 (Victor: "¿una sola API con earnings + opciones para pagar menos? Y si en vez de todo el mes/semana miramos los 2 días siguientes, así tengo los tickers pre/post del día siguiente"): sobre la fuente única — verificado que Alpha Vantage Premium ($49.99/mes) da HISTORICAL_OPTIONS (cadenas históricas a 2008) + earnings + precios con UNA key y sin tope, reemplazaría a Market Data, pero es MÁS caro que los $30 del Trader; NO se cambia nada de fuentes. La clave es su segunda idea: VENTANA CORTA del barrido. Nuevo selector en el paso 1 de capital (Hoy+mañana / 3 días / Semana, KEY_SCRWIN, por defecto 2 = hoy+mañana) que fija el rango del calendario (from hoy, to hoy+(win-1)d). Con 2 días quedan ~10-15 nombres tras el filtro de capital → ~10-15 llamadas de Alpha Vantage (1 por ticker para las fechas) < 25/día → CABE EN EL PLAN GRATIS de AV. O sea: Market Data Trader $30 + Alpha Vantage gratis + ventana 2 días = el barrido diario funciona sin pagar de más, y le da justo los tickers pre/post del día siguiente para preparar el trade. Textos del paso 1 y footer adaptados a la ventana; winDays guardado en el payload de capital. Verificado node (ventanas 2/3/7 dan los rangos correctos) + babel.

v2.08 (Victor, con el desglose de errores de v2.06: casi todos "sin earnings pasados en Finnhub" — el troceo de v2.07 confirmó que el plan GRATIS de Finnhub solo da earnings FUTUROS, no pasados; preguntó "¿se puede sacar de otro sitio?"): nueva fuente de FECHAS de earnings pasados = ALPHA VANTAGE (endpoint EARNINGS, gratis). Verificado en su web: FMP tiene fecha+pre/post+histórico pero su calendario de earnings es de PAGO; Alpha Vantage EARNINGS es gratis y UNA sola llamada trae TODO el histórico de un ticker (reportedDate + a veces reportTime pre/post), décadas atrás — límite 25/día · 5/min (de sobra para la sonda de un ticker, corto para barrer decenas). Nuevo fetchEarningsHistoryAV(sym, key) (detecta el límite: AV mete Note/Information dentro de un 200); reconstructHistory acepta avKey y lo usa para las fechas PASADAS si está puesta (Finnhub de respaldo), y el PRÓXIMO earnings sale de fetchNextEarnings de Finnhub (el futuro sí funciona gratis). Nueva key en Ajustes → Datos "Fechas de earnings (Alpha Vantage)" (KEY_AVANTAGE, en BACKUP_KEYS y en el backup JSON como avKey), con su modal y la nota de dónde sacarla. runProbe y runLiquidez leen la key AV y la pasan. Con la key puesta, la sonda de NFLX debe reconstruir ya; el barrido de decenas seguirá topando con el límite diario de AV (se ve en el desglose).

v2.07 FIX de la reconstrucción (Victor, captura: la sonda de NFLX daba "sin earnings pasados en Finnhub para NFLX" — la causa de que 29/30 fallaran): el calendario /calendar/earnings de Finnhub en plan gratis LIMITA el rango por llamada (~1 año; su propio ejemplo de docs usa ventana de 1 año), y reconstructHistory pedía 3,3 años DE GOLPE → Finnhub devolvía vacío → "sin earnings pasados" hasta para NFLX. fetchEarningsHistory ahora TROCEA la consulta en ventanas de ≤360 días (4 llamadas para 3,3 años) y junta los resultados con dedup por fecha (verificado con node: ventanas sin huecos ni solapes, todas ≤360d). Con fetchRetry (v2.06) cubriendo el 429. La sonda de un ticker debería funcionar ya; el barrido semanal hace ~4 llamadas Finnhub/nombre (con reintentos por el rate-limit de 60/min). Si aún saliera vacío, sería que el plan gratis dejó de servir histórico del calendario y habría que cambiar de fuente.

v2.06 (Victor, captura: "solo me ha dado un resultado filtrado, no puede ser" — el barrido midió 30 pero el resumen mostraba 1): NO era el filtro — 29 de 30 reconstrucciones FALLABAN en silencio (solo ICLR salió, y encima no pasa liquidez). Los errores estaban ocultos (se filtran de tarjetas y resumen), así que parecía "un solo resultado". DOS cosas: (1) VISIBILIDAD — el contador pasa a "N intentados · M con datos · K con error"; nuevo desplegable ámbar "N sin reconstruir — ver por qué" que agrupa los fallos por mensaje y lista los tickers, para diagnosticar (rate-limit / velas / cadena vacía / plan). (2) CAUSA PROBABLE = saturación de la API al encadenar ~25 llamadas por nombre: nuevo fetchRetry (reintenta ante 429/500/502/503 con espera creciente) cableado en fetchStockCandlesMD, fetchEarningsHistory y fetchMdExpirations (los que producían el error de reconstrucción); mensajes de error más concretos (velas no_data/vacías, Finnhub 429). Pendiente ver el desglose real en su navegador para afinar.

v2.05 FIX CRASH (Victor, con captura: "la app hace crash al darle al resumen del barrido"): el barrido se guarda en localStorage con JSON.stringify, y JSON convierte NaN → null. Cuando el barrido corre con NY cerrado (sin cadena del próximo → em NaN) o sin histórico reconstruido (pop/edge/emAvg NaN), esos campos vuelven como null al releer. La trampa: isFinite(null) === true en JS (Number(null)===0), así que el guardado isFinite(x) ? x.toFixed() : "—" dejaba pasar el null y null.toFixed() petaba al pintar el resumen (y podía petar las tarjetas con liquidez apagada). Arreglado cambiando los 82 isFinite( del Screener a Number.isFinite( (que sí devuelve false para null/undefined/NaN) — los campos nulos se pintan "—" en vez de reventar. Reproducido y verificado con render server-side + el round-trip JSON. Sin cambios de comportamiento salvo no petar.

v2.04 (Victor: "quítale el nombre de resumen por ticker moomoo"): el cuadro colapsable dentro de la herramienta Earnings pasa de titularse "Resumen por ticker (moomoo)" a solo "Resumen por ticker". Sin más cambios.

v2.03 (Victor: "sube el tope" tras entender el embudo 286→~36 capital→medidos): el tope del paso 2 del screener sube de 30 a 50 nombres. Con el plan Trader los créditos no son el límite (~27/nombre × 50 ≈ 1350 de 100.000/día); el freno es el tiempo del barrido (~25 llamadas encadenadas por nombre). El embudo real lo marca el filtro de capital (gratis, sin tope): con $10B quedaban ~36, así que 50 los cubre de sobra.

v2.02_TAG /* v2.02 (Victor, con trial de 30 días del plan Trader de Market Data: "quiero que el screener haga lo mismo que la herramienta de earnings — que dé el POP, que se pueda filtrar manualmente por liquidez (dato o no filtrar), market cap (mínimo) y POP (mínimo, lo dejaría en 75%), meter un único ticker para comparar con moomoo, tarjetas como earnings y el cuadro resumen"): RECONSTRUCCIÓN DEL HISTÓRICO DE POP desde Market Data. Nuevos helpers: fetchMdExpirations/fetchMdChain aceptan date (cadena HISTÓRICA, plan Trader, cadenas a 2005), fetchStockCandlesMD (velas diarias), fetchEarningsHistory (Finnhub earnings pasados), y reconstructHistory(sym) que por cada earnings pasado saca el GAP real de las velas (BMO: open vs cierre anterior; AMC: open del día siguiente vs cierre; sesión desconocida→BMO) y el EM que había (cadena histórica del día en que se abriría el trade, straddle ATM) → eventos en el MISMO formato del lector moomoo, así erStats/tarjetas/cuadro salen idénticos (verificado con node: gap BMO/AMC y erStats→POP 75%). SONDA de un ticker ahora RECONSTRUYE TODO: tiles POP/Ø edge/Ø EM/Peor + liquidez del próximo + comparación POP y EM vs moomoo (Δ POP verde ≤8 pts, Δ EM verde ≤0.5). Paso 2 del barrido semanal reconstruye POP por nombre (tope 30, ~27 créditos c/u) y trae FILTROS MANUALES: liquidez on/off + OI mín + spread máx, y POP mínima (selector, por defecto 75% — le dije que mejor ajustable que fijo); los filtros se aplican EN VIVO sobre lo ya medido (cambiar un selector re-filtra sin rebarrer). Tarjetas con tiles POP/edge/EM/N + Δ vs moomoo, agrupadas por fecha del próximo earnings; cuadro resumen recompilado con columnas POP/Ø edge/Ø EM/EM próx/Spr/OI/Mkt cap. Sobre el plan: la misma key de Market Data vale (Trader solo añade créditos + histórico). PENDIENTE validar en su navegador con la key Trader — la reconstrucción (fechas BMO/AMC, expiración histórica) no se ha probado contra la API en vivo; es justo el objeto de la comparación de 30 días.

v2.00 (Victor: "que haya funcionalidad de meter directamente un ticker y se baje los datos, así se compara rápido con moomoo; y puedes meter cuadro resumen también en la herramienta de earnings y así comparamos"): DOS AÑADIDOS. (1) SONDA DE UN TICKER en el Screener, arriba del todo: escribes un símbolo, "Bajar", y trae al instante fecha del próximo earnings (Finnhub fetchNextEarnings), EM del straddle ATM y liquidez de la zona (Market Data, ~1 llamada Finnhub + ~2 créditos); tarjeta con veredicto GO/JUSTO/NO, EM/spr/OI/vol, IC generado con el ala global, aviso de NY cerrado y, si el ticker tiene tabla moomoo, la comparación "moomoo ±X vs MD ±Y · Δ pts" EN EL ACTO — el cotejo rápido que pidió. Botón Abrir en Earnings. (2) CUADRO RESUMEN "Resumen por ticker (moomoo)" también DENTRO de la herramienta Earnings (colapsable, arriba, sobre el lector): las columnas del Summary by name (N, Dentro EM, Ø edge = ØEM−Ø|open|, Ø EM, Peor, Liq) derivadas del histórico, ordenable, tocar fila carga el ticker abajo — para comparar con moomoo sin cambiar de pestaña (es una VISTA del mismo histórico que ya está en el Screener; el de allí es del barrido Market Data, este es del histórico moomoo). Sobre pagar el plan Trader: CONFIRMADO en su docs que las cadenas históricas llegan a 2005 (coste con date = 1 crédito/1000 símbolos), así que con el plan de pago SÍ se puede reconstruir el histórico de POP por ticker (EM implícito del día antes de cada earnings pasado + move real) y jubilar las capturas de moomoo — se construye cuando Victor pague.

v1.99 (Victor: "haz que filtre por market cap, que eso sí que no varía; y un segundo botón de filtro de capital que solo mida las que hayan pasado el primero, y lo pase con mercado abierto") → SCREENER EN DOS PASOS SEPARADOS. PASO 1 "Filtro de capital" (gratis, solo Finnhub, a cualquier hora porque el market cap no cambia con la sesión): calendario de la semana + market cap de cada uno, y FILTRA por un UMBRAL AJUSTABLE en pantalla (selector $2B/$5B/$10B/$20B/$50B/$100B, por defecto $10B según eligió; guardado en KEY_SCRMINCAP); guarda los que pasan en KEY_SCRCAP y limpia el resultado de liquidez anterior. PASO 2 "Filtro de liquidez" (créditos de Market Data, deshabilitado hasta tener paso 1): mide EM y liquidez SOLO de los que pasaron el capital (tope 45 por el cupo), con chip NY abierto/cerrado y el aviso de sesión de v1.98. Así los créditos no se gastan en nombres que ni pasan el tamaño — el capital recorta la lista gratis y la liquidez, cara, ataca ya poco. runScreener se parte en runCapital + runLiquidez; el contador de resultados añade "N pasan capital". La POP en el screener queda PENDIENTE (necesita la serie histórica de gaps que Market Data gratis no da; hoy la POP vive en las tarjetas de Earnings vía histórico de moomoo).

v1.98 AJUSTE DEL SCREENER tras el primer barrido real de Victor (286 reportan · 36 pasan capital · solo 3 pasan liquidez — "el filtro es demasiado exigente, no filtres por capital e intenta sacar el dato de liquidez solamente; el cuadro de abajo que no sea de moomoo sino compilado con los datos de Market Data"): (1) el MARKET CAP YA NO FILTRA — se descarga igual (gratis) pero solo informa y decide qué 45 se miden primero (los mayores; el cupo de créditos no da para cientos): nadie queda fuera por capital, quedan fuera por cupo y se dice. (2) SE ENSEÑAN TODOS los medidos con su veredicto (✓ GO / JUSTO / ✗ NO) agrupados por día — el filtro informa, no esconde; contador verde/ámbar/rojo. (3) DIAGNÓSTICO del "demasiado exigente": el barrido fue a las 6:05 ET con NY CERRADO — la caché sirvió spreads de cierre/preapertura (MS con spr 12% y OI 355, imposible en sesión) y el veredicto castigó de más; nuevo helper nyOpen() (Intl con America/New_York, 9:30–16:00 lun–vie) guarda marketOpen en el payload y los resultados llevan aviso ámbar pidiendo re-escanear en sesión (15:30–22:00 peninsular). (4) La tabla de abajo pasa de "Resumen por ticker (moomoo)" a "RESUMEN DEL BARRIDO (Market Data)": todas las filas medidas del último escaneo — Símbolo/Día·sesión/Liq/EM/Spr/OI/Vol zona/Mkt cap — ordenable (por defecto veredicto asc, GO primero), tocar fila abre en Earnings con spot/EM/IC cargados; la tabla moomoo se retira de la pestaña (su histórico sigue en las tarjetas de Earnings y en la comparación de EM de cada tarjeta del screener) y con ella KEY_MCAP/loadMcaps/saveMcaps (código muerto fuera).

v1.97 FIX LOGO (Victor: "ponme logo" — la pestaña Screener salía con el icono VACÍO): la v1.96 le pasaba icon="refresh" al selector de Herramientas, que pinta con ActIcon, donde ese nombre no existe — el mismo fallo que la campana de v1.75 (SettingsRow/SetIcon sí tiene refresh; ActIcon no). Nuevo icono "filter" (embudo de filtrado, mismo trazo de 2px que el resto del set) en ActIcon, usado en la pestaña Screener del selector y en la cabecera de la herramienta.

v1.96 REESTRUCTURA (Victor: "no me has entendido — deja la herramienta de earnings como estaba antes y crea un menú distinto para el screener, un tiempo comparando entre los datos que subo de moomoo y los de Market Data; listado por día de las que pasen el filtro, toda la semana, con las tarjetas debajo y el generador de spreads; automatizar al máximo pero validar primero"): (1) EARNINGS VUELVE A COMO ESTABA (pre-v1.94): fuera de dentro el screener y la tabla-resumen; se queda lector + tarjetas + calculadora, más un receptor de saltos (KEY_ERJUMP: patch pendiente que aplica al montarse — histórico del ticker con applyHist si lo hay + spot/EM/strikes del screener). (2) NUEVA PESTAÑA "Screener" en Herramientas (Puts | Earnings | Screener | Alertas, KEY_TOOLSUB acepta "screener"). ScreenerToolContent: barre la SEMANA (fetchEarningsCal con rango from/to — el calendario de 7 días en UNA llamada Finnhub, cada fila con su date), gate market cap gratis <$5B, supervivientes (tope 45 por el cupo, los mayores; se avisa si trunca) → Market Data en caché ~2 créditos c/u. LISTADO POR DÍA de los que PASAN (verde/ámbar; descartados solo en el contador), tarjetas con TKR/sesión/mcap/veredicto/EM/spr/OI/vol + IC generado con SELECTOR DE ALA global (Auto = 2 escalones, o 2/2.5/3/5/10 — recalcula en vivo sin refetch) + botón "Abrir en Earnings" que salta con todo cargado. (3) VALIDACIÓN moomoo vs Market Data, el corazón del encargo: si el ticker tiene tabla moomoo con ± pendiente (nextEm de erStats), la tarjeta enseña "moomoo ±X% vs MD ±Y% · Δ pts" coloreado (verde ≤0.5 · ámbar ≤1.5 · rojo más); sin tabla lo dice y pide subirla. (4) La tabla "Resumen por ticker (moomoo)" se MUDA a esta pestaña (es el lado moomoo de la comparación); tocar fila salta a Earnings con applyHist. El barrido se cachea (KEY_SCREEN con from/to y aviso de antigüedad).

v1.95 (Victor, con captura del "Summary by name" de moomoo: "lo ponemos en menú aparte para comparar frente a moomoo — un listado parecido a esto y luego las tarjetas"): TABLA-RESUMEN por ticker arriba de la herramienta Earnings (colapsable "Resumen por ticker · comparar", encima del screener y de las tarjetas). Una fila por ticker del histórico con las columnas del cuadro de moomoo, TODAS derivadas de lo que la app ya tiene: N (eventos), Dentro EM (popOpen), Ø edge = ØEM − Ø|open| (= el "Avg P&L" de moomoo, verificado al decimal con el ASML de Victor: 5.9 − 5.95 ≈ −0.05), Ø EM, Peor gap, Liq (OI mediano del último chequeo de liquidez v1.90, coloreado por veredicto) y Mkt cap (Finnhub /stock/profile2, cacheado en KEY_MCAP re-derivable, botón "Cargar market caps" que trae solo los que falten, pausa 220ms). Cabeceras TOCABLES para ordenar (desc↔asc; por defecto market cap desc como la captura); scroll horizontal para las 8 columnas en móvil con la de símbolo sticky. Tocar una fila hace applyHist + navega el carrusel a esa tarjeta → el "listado y luego las tarjetas" que pidió. Sin dato va al final del orden y como "—" (nunca inventado). Reutiliza erStats, liqVerdict y fetchProfile (de v1.94); helpers loadMcaps/saveMcaps.

v1.94 (Victor: "que use todos esos datos para proponer trades y filtrar los que no valgan día a día sin tener que subir nada" → eligió barrer TODOS los que reporten): SCREENER AUTOMÁTICO DE EARNINGS, sección colapsable "Screener de hoy" arriba de la herramienta Earnings. Pipeline CREDIT-SAFE de dos fases: (1) GATE BARATO con Finnhub (gratis) — fetchEarningsDay (/calendar/earnings sin símbolo = todos los US del día, verificado en plan gratis) + fetchProfile (/stock/profile2, market cap en millones) por ticker con pausa 220ms; descarta <$5B sin gastar créditos. (2) solo los supervivientes tocan MARKET DATA (fetchMdExpirations + fetchMdChain en caché, ~2 créditos c/u): EM del straddle ATM (emFromChain, solo strikes con dos lados), liquidez de la ZONA ±1.5×EM (liqStats/liqVerdict reutilizados) y strikes IC propuestos (±EM redondeados, ala = 2 pasos). Barrido de ~40 nombres con ~12 supervivientes ≈ 24 créditos del cupo de 100/día. Resultados ORDENADOS (verde>ámbar>rojo, luego market cap desc; errores al final), cacheados por fecha (KEY_SCREEN, re-derivable, NO backup) para no rebarrer, con barra de progreso por fase. Cada fila: TKR + sesión + market cap (ámbar $5-10B) + veredicto GO/JUSTO/NO + EM + spr% + OI + vol de zona + IC propuesto; tocar carga ticker/spot/EM/strikes en la calculadora. Nota honesta: EM y liquidez son del vto que cubre el gap, no de la cadena entera. Necesita las dos keys (Finnhub + Market Data).

v1.93 FIX DE CRÉDITOS (al revisar la docs de Market Data por la pregunta de Victor sobre el plan Trader): la v1.92 asumía "2 peticiones por chequeo", pero en options/chain EN VIVO cada CONTRATO de la respuesta consume 1 crédito — un solo chequeo (~60-150 contratos del vencimiento) podía fundirse el cupo gratis de 100/día entero. Arreglo con su propio mecanismo: fetchMdChain pide primero mode=cached (la llamada ENTERA cuesta 1 crédito, datos de hace segundos/minutos — para medir liquidez sobra; 203 = OK desde caché, 204 = sin caché y sin cargo) y solo cae a EN VIVO si no hay caché o el plan no lo soporta. El pie del panel declara la procedencia real de cada lectura ("caché, ~2 créditos por chequeo" o "en vivo: cada contrato consume 1 crédito") y el modal de Ajustes corrige el wording (100 CRÉDITOS/día, no peticiones). Con caché: ~2 créditos por chequeo → ~50 chequeos/día en el plan gratis, como se pretendía.

v1.92 FIX DE FUENTE (captura de Victor: "Liquidez: el navegador bloqueó la llamada a CBOE (CORS)"): el riesgo avisado en v1.90 se confirmó, Y ADEMÁS la página de delayed quotes de CBOE prohíbe expresamente la extracción automática (amenaza con bloquear IPs) — un proxy CORS para saltárselo sería construir sobre terreno prohibido y frágil: DESCARTADO. La liquidez pasa a MARKET DATA (api.marketdata.app), una API PARA desarrolladores: token gratis con 100 peticiones/día, cadena de opciones con bid/ask/OI/volumen y filtro por vencimiento. Cada chequeo gasta 2 peticiones (GET /options/expirations/TKR + GET /options/chain/TKR?expiration=YYYY-MM-DD) → ~50 chequeos/día, de sobra. NUEVA KEY en Ajustes: fila "Liquidez opciones (Market Data)" bajo la de Finnhub, con su modal (KEY_MDATA en localStorage; entra en BACKUP_KEYS, en el export JSON v12 como mdataKey y en el import — mismas rutas que la de Finnhub); getLiq la lee de localStorage al pulsar, así el guardado vale sin recargar, y sin key el panel dice dónde activarla. fetchMdExpirations/fetchMdChain sustituyen a parseOcc/fetchCboeChain (errores mapeados con honestidad: 401/403 key inválida, 402/429 cupo agotado); el botón pierde el "(CBOE)" y el pie del panel declara la fuente y el gasto de cupo. Todo lo demás — pickErExpiry, zona ±1.5×EM, mediana, 4 patas, fricción, badge pasa/no-pasa, desplegable de criterios — intacto.

v1.91 (Victor, tras preguntar por la liquidez mínima: "pon si pasa el filtro en la tarjeta y abajo un desplegable de liquidez con un botón de info explicando lo anterior con los rangos de liquidez y market cap"): (1) el badge de la tarjeta pasa a PASA/NO-PASA explícito — "✓ PASA LIQUIDEZ" (verde: spread ≤5% y OI ≥100), "LIQUIDEZ JUSTA" (ámbar) o "✗ NO PASA" (rojo: spread >15% u OI <10), con los mismos spr%/OI/fecha de siempre. (2) Al PIE de la herramienta, desplegable plegado "Criterios de liquidez mínima ⓘ" con los rangos completos: spread por pata (≤5% verde · 5–15% justo · >15% no operable con dit=1 — la fricción se paga dos veces), fricción vs crédito (≤15% bien · 15–30% justo · >30% NO-GO aunque la POP acompañe), OI por strike (≥100 suelo · ≥500 cómodo · <50 el bid×ask de pantalla se evapora), volumen de opciones (≥5–10k contratos/día), MARKET CAP (≥$10B cómodo · $5–10B caso a caso, decide el chequeo · <$5B fuera), volumen de la acción (≥1–2M acc/día — los MM cubren delta con el subyacente) y semanales imprescindibles (un vto lejano deja extrínseco tras el IV crush que se come el edge del dit=1). Nota honesta al pie: el chequeo CBOE MIDE spread/OI del vto real; market cap y volúmenes son criterio de lista, la app no los descarga — rangos, no semáforo.

v1.90 (Victor: "¿puedes sacar datos de liquidez de las opciones para filtrar los earnings?" → "las dos cosas"): LIQUIDEZ DE OPCIONES desde el JSON abierto de CBOE (cdn.cboe.com/api/global/delayed_quotes/options/TKR.json — cadena completa con bid/ask/OI/volumen, sin key, 15 min de retraso; si el navegador bloquea CORS la UI lo dice y no se inventa nada). Botón "Comprobar liquidez (CBOE)" en la herramienta Earnings: elige el vencimiento que CUBRE el gap (BMO → primer vto ≥ calDate; AMC → vto > calDate, porque el del mismo día expira antes del gap; sin fecha, desde hoy), mide la ZONA OPERABLE (strikes a ±1.5×EM del spot, ±10% sin EM) con MEDIANA del spread bid-ask en % del mid y OI mediano (mediana, no media: un strike muerto en la cola no tumba el veredicto), y localiza las 4 PATAS escritas — bid×ask, spread %, OI y volumen por pata, con "strike no existe en ese vto" o "sin cotización a dos lados" cuando toca. FRICCIÓN ida+vuelta = Σ spreads de las 4 patas (entrada+salida al mid), en $ y como % del crédito (rojo >30%, ámbar >15%): la métrica que decide si el cóndor es ejecutable aunque la POP sea buena. Veredicto LÍQUIDO/JUSTO/ILÍQUIDO con umbrales A LA VISTA (verde ≤5% y OI ≥100 · rojo >15% u OI <10). El resumen se guarda en el histórico del ticker (KEY_ERHIST → entra en el backup) y la TARJETA del carrusel lleva el badge: veredicto + spr% + OI, con la fecha de la lectura si no es de hoy. Helpers: parseOcc (símbolo OCC → vto/tipo/strike), fetchCboeChain, pickErExpiry, liqStats, liqVerdict.

v1.89: sube el número de versión (petición de Victor) — sin cambios funcionales sobre la v1.88.

v1.88 (Victor, tres pasadas hasta dejarlo fino): en la lista de posiciones del Histórico (Por estrategia), las filas de ACCIÓN (natOf ACC) cambian dos cosas. (1) El ÚLTIMO PRECIO del subyacente va dentro de la sublínea de la cantidad, a la izquierda del nº de acciones y en su mismo tamaño (11.5px, tabular): "142.35 · × 30 acc · 12 may 25" — sin rótulo, y solo si hay precio del último 🔄 Precios (nunca inventado). (2) La CIFRA DE LA DERECHA de una acción ABIERTA con precio pasa a ser el UNREALISED (effPnl = (último − BEP) × qty, verde/rojo) EN LUGAR del realizado — que salía +$0 en verde hasta la primera venta y no decía nada. Sin rótulo extra. Cerradas siguen con el realizado (mtmSumOf) y las opciones con lo suyo de siempre; el detalle expandido no cambia.

v1.87 FIX (captura de Victor: la tarjeta de STT sin fecha de release mientras el formulario SÍ la enseñaba tras "Obtener spot" — Finnhub tenía el dato): DOS fallos del backfill v1.77. (1) calChecked se marcaba SIEMPRE, también cuando la LLAMADA FALLABA (p.ej. 429 del rate limit al encadenar una consulta por tarjeta) — el ticker quedaba bloqueado sin fecha hasta el día siguiente; fetchNextEarnings distingue ahora TRES estados (dato / OK-sin-eventos {none} / fallo null) y el fallo NO marca calChecked (reintenta en la próxima apertura); pausa entre llamadas 150→250ms para dar aire al rate limit. (2) El dato que "Obtener spot" YA traía se quedaba en el strip del formulario sin guardarse: ahora, si el ticker tiene tarjeta, se guarda en su histórico (calDate/calHour/calChecked) — el caso de la captura se arregla con el gesto que Victor ya hizo. El lector de tablas también exige cal.date antes de guardar (con {none} guardaba undefined).

v1.86 (Victor: "ordena las tarjetas de earnings por fecha de release, pre antes que post si es el mismo día"): el carrusel pasa de orden ALFABÉTICO (v1.38) a CRONOLÓGICO por el próximo earnings — la tarjeta cuyo earnings toca antes va primera (calDate del calendario Finnhub guardado en el histórico, el que rellena el backfill de v1.77). En el MISMO DÍA manda la sesión en su orden real de publicación: BMO/pre (8:00-9:30 ET) antes que EN SESIÓN antes que AMC/post (16:00+ ET); sin sesión conocida, al final del día. Tarjetas SIN fecha futura conocida van al final en alfabético — no se inventa un orden con una fecha que no existe. El sort es estable con el resto de la lógica: SnapCarousel recibe las mismas keys reordenadas y la sincronía activeKey no cambia.

v1.85 FIX (Victor, con captura: "PPI aparece mañana pero hoy es 15"): la cuenta atrás de Eventos Macro USA hacía Math.ceil((evento − ahora)/86400000) — a las 13:42, el PPI de HOY a las 14:30 daba 0.03 días → ceil → 1 → "mañana", falso. Nuevo macroCalDays(t): diferencia en DÍAS DE CALENDARIO en hora local (ambas fechas a medianoche) — mismo día = "hoy" aunque falten 10 horas o haya pasado hace 2; corregido en las filas y en el resumen de la cabecera plegada, que tenía el mismo fallo.

v1.84 (Victor): el botón "Esperado vs publicado" de cada evento macro pasa de Investing a FOREXFACTORY (forexfactory.com/calendar) — su calendario lleva forecast/previous/actual por evento y es la referencia de la comunidad; la etiqueta lo dice: "Esperado vs publicado (FF)".

v1.83 (Victor: "¿puedes descargar el esperado vs publicado para que se vea al pulsar en cada uno?"): DESCARGARLO no, con honestidad — el "esperado" (consenso) es dato de encuestas PRIVADAS (Bloomberg/Reuters) sin fuente gratuita fiable para un navegador, y la API pública de BLS (el publicado) bloquea CORS; integrar feeds no documentados (p.ej. el semioficial de ForexFactory) es colgarse de algo que revienta sin avisar. En su lugar, cada evento de "Eventos Macro USA" es TOCABLE y despliega dos enlaces de un toque: la FUENTE OFICIAL donde el publicado aparece en el segundo en que sale (BLS cpi/ppi/empsit.nr0.htm — URLs fijas de décadas — y el statement de la Fed) y el calendario de Investing con esperado vs publicado vs anterior. Puerta futura anotada: si el screener del VPS publica forecast/actual en su screener.json, la app los pintará de ahí.

v1.82 (Victor): la tarjeta macro de Vencimientos pasa a llamarse "Eventos Macro USA" y es COLAPSABLE — cabecera tocable con chevron, y el estado se recuerda entre sesiones (localStorage bloques_macro_open; preferencia de UI local, no entra en backup). Plegada no se calla: la cabecera enseña el PRÓXIMO evento con su cuenta atrás ("CPI 12 ago · en 28 d", rojo si ≤1 día) — el 80% del valor en el 20% del espacio.

v1.81 (Victor: "integra automáticamente para DC y DD que se anote si el trade entra dentro de uno de esos eventos"): el calendario macro embebido (v1.80) se conecta a la ficha DC/DD, que YA tenía catalizadores manuales (eventIn/event/eventTxt) — ahora se anotan solos. Tres piezas: (1) helper macroBetween(from,to) — eventos del calendario oficial cuya fecha cae en la ventana, extremos INCLUSIVE (un CPI a las 8:30 ET del día de vencimiento cae dentro). (2) WIZARD: con el vto corto relleno, strip en vivo — ámbar "Macro dentro del vto corto: CPI 12 ago · FOMC 16 sep — se anotará en la ficha" o neutro "sin CPI·PPI·NFP·FOMC dentro"; al GUARDAR, si el usuario no marcó nada a mano y hay eventos, la ficha se anota sola: eventIn=SI, event=primer tipo (PPI se mapea a "Otro": DCDD_EVENTS no lo lista), eventTxt="auto: " + lista completa; si marcó SI a mano, manda lo suyo, sin pisar nada. (3) TARJETA de posición DC/DD abierta: strip vivo "⚡ Queda dentro del vto corto: …" con lo que AÚN queda entre hoy y el vencimiento corto, recalculado del calendario — la tile Evento es la foto al abrir, el strip es lo que tienes por delante; posiciones cerradas no lo pintan. Todo sale de las fechas oficiales BLS/Fed embebidas: sin API, sin inventar.

v1.80 (Victor: "¿puedes sacar economic calendar tipo CPI PPI NFP desde Finnhub?"): de Finnhub NO — /calendar/economic es premium y su key gratis no lo trae. Solución mejor sin API: BLS y la Fed publican las fechas OFICIALES con un año de antelación, así que van EMBEBIDAS (verificadas el 15 jul 2026 en bls.gov/schedule y federalreserve.gov): CPI, PPI y NFP (Employment Situation) a las 8:30 ET y las 4 decisiones FOMC restantes de 2026 a las 14:00 ET (16 sep y 9 dic con dot plot). Cada evento lleva su instante UTC exacto (EDT jul-oct, EST nov-dic ya resuelto al construir la lista) y la tarjeta pinta la HORA LOCAL del dispositivo — nada de conversiones en vivo frágiles. Nueva tarjeta "Macro USA" arriba de la vista Vencimientos: próximos 5 eventos con chapa por tipo (CPI ámbar, PPI neutro, NFP morado, FOMC rojo), fecha + hora local, nota ("IPC junio", "decisión + dot plot") y cuenta atrás (hoy/mañana/en N d, rojo si ≤1 día); el evento del día sigue visible 2h tras salir. La lista cubre hasta dic 2026: agotada, la tarjeta PIDE la actualización en vez de inventar fechas de 2027. Sin key, sin límites, sin CORS.

v1.79 (Victor: "en Vencimientos las long call con el spot por debajo del BEP hay que marcarlas en rojo, no en verde"): el semáforo coloreaba TODO por la regla de opciones cortas (put en rojo si last<strike, call en rojo si last>strike) — para una LONG CALL eso está invertido: spot bajo el strike salía VERDE cuando significa expirar sin valor (pérdida total de la prima), y por encima del strike salía rojo cuando es donde empieza a ir bien. Nuevo helper vencDanger, único para resumen y filas: cortas igual que siempre (peligro = asignación), LONG CALL contra su BEP (strike + prima) — last < BEP → ROJO, last ≥ BEP → verde, sin precio → sin color (el BEP es el punto donde la posición de verdad gana; entre strike y BEP recupera algo pero pierde neto, y eso es rojo). Las tiles del resumen se renombran de "Opciones ITM"/"Riesgo ITM" a "En peligro"/"Riesgo en peligro": una long call bajo el BEP cuenta como en peligro y llamarla ITM era falso.

v1.78 (Victor: "que sea más fácil borrar las tarjetas de earnings"): el pie de cada tarjeta pasa de DOS botones granulares ("✕ idea" y "✕ tabla", cuatro toques para quitar un ticker) a UN solo "✕" que borra la TARJETA ENTERA — idea + tabla del ticker — con la confirmación de dos toques de siempre ("¿Borrar todo?" en rojo). dropIdea/dropHistTkr siguen en el código por si se quiere recuperar el granular; borrar la tarjeta y releer la tabla (o reguardar la idea) repone cualquier mitad.

v1.77 (Victor: "mete que se vea en la tarjeta la fecha de earnings y pre o post"): la v1.73 ya pintaba fecha + sesión en la tarjeta, pero solo guardaba calDate/calHour AL LEER una tabla — las tarjetas ya existentes nunca lo enseñaban. BACKFILL al abrir la herramienta: para cada ticker del histórico sin fecha futura se consulta el calendario de Finnhub una vez al día (calChecked por ticker evita repetir consultas; 150ms entre llamadas; sin dato no se pinta nada, nunca se inventa). Las tarjetas viejas se rellenan solas al abrir la herramienta con key de Finnhub, y las fechas ya pasadas se renuevan tras cada earnings.

v1.76 (Victor): en la tarjeta de earnings, la etiqueta "Ø|open| / ØEM" (y "Ø IV crush") se cortaba con elipsis — el tile es un tercio de tarjeta y la etiqueta llevaba nowrap; ahora puede partir en dos líneas (whiteSpace normal, 9px, lineHeight 1.2) y se lee completa; el valor sigue en una línea.

v1.75 (Victor, con captura): (1) la fila "Notificaciones de alertas" salía con el cuadrado del icono VACÍO — le pasaba icon="bolt", que es del set ActIcon; SettingsRow pinta con SetIcon, donde no existía → nuevo icono "bell" (campana, mismo trazo que el resto) y la fila lo usa; el subtítulo del estado bloqueado se acorta a "Bloqueadas — actívalas en Ajustes de iOS" para que no se trunque en una línea. (2) El último precio de las alertas armadas pierde la hora (quedaba "último 6,842.10 · 14:32"; ahora solo el precio, a petición de Victor).

v1.74 FIX CRÍTICO (Victor: "al pulsar Ajustes la app hace crash y ya no abre"): el bloque notifPerm/askNotifPerm de la v1.72 cayó dentro de ComparadorContent — el archivo tiene DOS "const [confirmClear" y el parche ancló en el primero; la fila de Ajustes lo usaba desde App → ReferenceError al pintar Ajustes, y como KEY_VIEW recuerda la última pestaña, la app arrancaba en Ajustes y reventaba AL ABRIR. Movido a App. LOS DATOS NUNCA CORRIERON PELIGRO: viven en localStorage/IndexedDB y un fallo de pintado no los toca — el crash bloqueaba la puerta, no la caja. Además, PANTALLA DE RESCATE permanente (RescueBoundary envolviendo <App/>): si cualquier render futuro revienta, en vez de pantalla en blanco sale una tarjeta independiente del árbol roto con "Abrir en Portfolio" (borra KEY_VIEW y recarga) y "Descargar copia de emergencia" (BACKUP_KEYS de localStorage+IndexedDB → JSON v:"rescue-raw"); el importador de Backup acepta ese formato y lo restaura tal cual. Un bug de UI ya no puede volver a secuestrar ni la app ni los datos.

v1.73 — rediseño UX de la herramienta Earnings, seis peticiones de Victor: (1) el LECTOR DE TABLAS sube ARRIBA del todo (era lo último y es el primer gesto: subir la tabla → sale la tarjeta). (2) Tras leer una tabla o guardar una idea, el carrusel NAVEGA SOLO a esa tarjeta (setSyncTkr con el ticker guardado; en lote, el del campo si está o el primero). (3) HIGHLIGHT de selección: la tarjeta cuyo ticker está cargado en el formulario lleva borde de 2.5px del color de su veredicto + check junto al TKR. (4) MEDIAS ACLARADAS: los mini-tiles pasan a "Ø IV crush" y "Ø|open| / ØEM", y un botón ⓘ en el pie despliega la leyenda dentro de la tarjeta (Ø = media de los N earnings; si Ø|open| < ØEM, vender el EM tuvo colchón; peor gap = mayor open; POP = % dentro del EM, suavizada). (5) FECHA DEL PRÓXIMO EARNINGS + SESIÓN en la tarjeta: al leer la tabla se consulta el calendario de Finnhub por cada ticker y se guarda calDate/calHour en su histórico — la tarjeta enseña "mar 14 oct · POST · abre ese día" (PRE en ámbar: "abre la tarde antes"); fechas pasadas no se pintan y sin dato no se inventa. (6) PARTE DE ABAJO simplificada: sección renombrada "Probabilidad · histórico", campos M/X/U PLEGADOS tras "Editar histórico a mano" (casi siempre los rellena el lector), y FUERA las 4 barras del plan B (Victor: "prácticamente dice cierra siempre" — con POP altas iban todas llenas e iguales: cero información y duplicaban las 4 tiles); el plan B queda con su veredicto y UNA cifra que sí decide: la reversión intradía (close − open) en puntos, verde si los gaps se corrigen.

v1.72 — alertas, ronda 2: (1) NOTIFICACIONES EN AJUSTES (Victor: "no le di al aviso y ya no salta"): fila "Notificaciones de alertas" en General con el estado real del permiso — default (descartado sin responder): toca y vuelve a pedir; granted: toca y manda una notificación de PRUEBA; denied (le dio a No permitir): el navegador BLOQUEA re-pedirlo desde la web — la fila lo dice y apunta a Ajustes del sistema → Notificaciones, que es el único sitio donde se desbloquea; no se finge un botón que no puede funcionar. (2) ÚLTIMO SPOT en las alertas armadas: evalAlerts guarda lastPx/lastAt en TODA alerta con cotización (dispare o no), la tarjeta enseña "último 6,842.10 · 14:32" o "sin cotizar aún"; "Comprobar ahora" cotiza también las disparadas para refrescar su spot (sin re-disparar). (3) NOTAS por alerta (campo opcional al crear, visible en armadas y disparadas — "vender la put 6800"). (4) RETRACEMENT ACLARADO (pregunta de Victor: "¿desde el máximo del día? ¿no debería ser desde el close anterior?"): son DOS medidas distintas y ahora hay una alerta para cada una — "Retr. máx día" = pullback intradía desde el MÁXIMO del día (un gap up +2% que cede al +0.5% ha retrocedido 1.5% aunque el día siga verde), y NUEVO tipo "Caída vs ayer" = dp de Finnhub desde el CLOSE ANTERIOR (ese mismo ejemplo sigue +0.5% y NO dispara); las etiquetas nombran la referencia para que no haya que recordarla.

v1.71 — cinco arreglos/añadidos: (1) FIX CRASH en Alertas (Victor: "al meter un ticker la app hace crash"): pf solo existía dentro de la herramienta Earnings; con el ticker vacío el disabled cortocircuitaba en !tkr.trim() y pf no se evaluaba — al teclear la PRIMERA letra se evaluaba pf(level) → ReferenceError → pantalla en blanco; pf local en AlertsToolContent. (2) ENCUADRE de CAGR · Máx DD · MAR (Victor: "mal encuadrados"): estaban DENTRO del flex del encabezado compitiendo con el título por la fila; pasan a bloque propio a TODO EL ANCHO bajo el título, tiles a 15px; el título Rendimiento sube de 15 a 17 ("¿el typo es pequeño?"). (3) FUERA el botón "+ Snapshot de hoy" (pregunta de Victor "¿sigue haciendo falta?" — no): cada 🔄 Precios guarda el snapshot del día con precios recién traídos; el manual sin refrescar grababa los precios RANCIOS del último refresco como si fueran de hoy — peor que no guardar; el texto de "sin snapshots" lo explica. (4) FIX encuadre de los date inputs del filtro de estrategias: sin appearance:none iOS pinta el date input con su cromo nativo y se salía del marco; mismo patrón que dateInputStyle + flex centrado. (5) CALENDARIO DE EARNINGS de Finnhub (pregunta de Victor: "¿puedes sacar calendario y si es pre o post market?" — sí, /calendar/earnings del plan gratis trae date y hour bmo/amc/dmh): fetchNextEarnings(symbol) busca el próximo en 60 días; "Obtener spot" lo trae junto al precio y un strip bajo el ticker enseña fecha + chapa PRE-MARKET (ámbar, "abre la tarde antes") / POST-MARKET (verde, "abre ese día") / EN SESIÓN — la sesión decide cuándo se abre el trade con dit=1; sin dato en 60 días lo dice, nunca inventa fecha.

v1.70 (Victor: "pon también que se vea drawdown"): el drawdown pasa de ser solo la tile Máx DD a tener su propia CURVA en el gráfico de Rendimiento — nueva métrica "Drawdown" en la fila de riesgo del selector (Riesgo · Drawdown · Apal. · EL/Margen · EL/NLV): % bajo el máximo histórico de la serie TWR en cada momento (0% = en máximos; el valle más hondo coincide con la tile). Se calcula sobre la serie COMPLETA y se recorta al rango después — el pico de referencia es el de toda la historia, nunca el del rango visible (un drawdown re-basado al mes mentiría). Sin Δ de pie (comparar drawdowns entre dos fechas no dice nada). La tile "Máx drawdown ▸" es ahora tocable y abre la curva.

v1.69 — cinco frentes: (1) HISTÓRICO DE ESTRATEGIAS: filtro por INTERVALO DE FECHAS para las cerradas (Desde/Hasta sobre la fecha de CIERRE, en el panel de filtros junto a ticker y resultado; Limpiar los resetea y el export JSON refleja el intervalo). (2) ALERTAS DE PRECIO — nueva herramienta en Herramientas (Puts | Earnings | Alertas), KEY_ALERTS con backup: Target (last ≥ nivel), Entrada (last ≤ nivel) y Retracement (caída desde el MÁXIMO DEL DÍA ≥ X% — el "avísame si SPX cede un 1%"; fetchQuote ahora trae h/l del día). Se evalúan con cada 🔄 Precios (todos los tickers de cartera) y con "Comprobar ahora" (que cotiza también tickers con alerta fuera de cartera); cada alerta dispara UNA vez (firedAt/firedVal) y se rearma a mano. HONESTIDAD SOBRE PUSH: sin servidor NO hay push con la app cerrada (Web Push necesita service worker + endpoint + VAPID firmado en servidor) — la Notification del sistema es best-effort con la app abierta (iOS la ignora fuera de PWA instalada) y la UI lo dice; el registro fiable es el de dentro de la app. (3) MAR RATIO en Rendimiento (decisión: ahí, y sobre la serie TWR sin aportaciones, no sobre NLV): CAGR anualizado, máx drawdown y MAR = CAGR/|maxDD|, SIEMPRE del histórico completo (no del rango visible — un MAR de 3 meses no significa nada) y solo con ≥60 días de historia (anualizar menos es inventar; la tarjeta lo dice); MAR verde ≥1, ámbar ≥0.5; sin drawdown aún, "—" en vez de un número infinito. (4) CADENAS DE ROLADOS con AÑO: shortDate local de RollChainTable pasa a "12 may 25" — una cadena puede cruzar diciembre; el resto de la app mantiene la fecha corta. (5) FIX modo noche en el gráfico de Rendimiento: el tooltip de fecha/valor era texto "#fff" fijo sobre rect T.ink — blanco sobre casi-blanco en oscuro; el texto pasa a T.card, que contrasta con T.ink en los dos modos.

v1.68 (Victor: "no quería que los ensancharas, solo ponerlos más altos — que estén como antes"): los movers vuelven a las DOS COLUMNAS de siempre (Ganadores | Perdedores, tarjetas de media pantalla); el último precio se queda bajo el ticker (solo número, sin $, 2 decimales, tabular) y la tarjeta crece en ALTURA, no en anchura. El precio lleva elipsis propia por si un índice de 5 cifras compite con el importe en columna estrecha; ticker e importe recuperan sus tamaños de la v1.66 (14/16).

v1.67 (Victor): los movers enseñan el ÚLTIMO PRECIO del subyacente bajo el ticker — solo el número, sin $, con 2 decimales y dígitos tabulares (es el `last` que dejó el último 🔄 Precios en las posiciones de ese ticker; sin refresco del día no se pinta, nunca un precio viejo como si fuera de hoy). Para que quepa con buen formato, las tarjetas pasan a ANCHO COMPLETO: Ganadores y Perdedores apilados en vez de a dos columnas — con ticker + precio + chapa % + importe, media pantalla se quedaba corta. Columna izquierda: ticker / precio / chapa %; derecha: el $ de tu posición; barra proporcional al pie, todo como estaba.

v1.66 — seis frentes de Victor en una tarde: (1) FIX tickers caros (su IC de ASML, "no calcula bien con patas de 10"): fmtK generaba "1,640" con separador de miles y el parser lo leía como 1.64 — el ancho de CUALQUIER subyacente de más de $1,000 salía 0.01 y el sizing entero era basura; no era el ala de 10, era ASML. fmtK sin agrupación + parseStrikeTok que aún entiende "1,640" guardado en ideas viejas (coma+3 dígitos = miles; si no, coma decimal). (2) Lector de tablas: 12 capturas por lote (antes 6); el carrusel ya acumulaba sin tope — ahora lo dice. (3) Lector del wizard para IC/Earnings: la rama de credit spreads devolvía SOLO el ticker (todo a mano); ahora las 4 ejecuciones del bróker (2 Sold + 2 Bought) se mapean enteras — spreads put/call, crédito neto = Σ vendidas − Σ compradas, comisión = Σ fees, contratos y vencimiento; Broken Wing/Credit Spread (2 patas) en la misma lógica. (4) Strikes con stepper −/+ en la herramienta Earnings: mueven el corto un escalón del bróker (putOff/callOff, persistidos con el formulario y las ideas) y TODO recalcula en vivo — % de margen real, aviso de asimetría, panel del ala, comparador, sizing y EV; Generar spreads resetea al EM. (5) El backup manual (JSON v12) por fin lleva la herramienta de earnings: histórico de tablas, ideas, formulario en curso y riesgo objetivo — solo viajaban en la copia automática de versión, exportar a otro dispositivo los perdía; restaurar un v11 sigue funcionando. (6) Plan B arreglado (Victor: "siempre dice cierra"): con 0 roturas ambas POP son 100%, revert=0 y caía en CIERRA con el texto absurdo "100% vs 100%: los gaps se extienden" — nuevas ramas SIN CASOS (0 roturas: no hay nada que aprender, el marco manda) y SIN SEÑAL (cierre ≈ open); CIERRA queda para cuando el histórico de verdad extiende los gaps. (7) Últimos 6 earnings: SE PONE EL DATO sin ponderar el modelo (con 6 puntos una ponderación mete más ruido que señal) — strip "X/6 fuera · |open| reciente vs total" que se vuelve ámbar con aviso si la tasa de roturas reciente supera claramente la histórica (cambio de régimen: la POP larga engaña al alza).

v1.65 REVERT (decisión de Victor: "un cóndor tiene que ser igual que cualquier B3, no se contabiliza prima hasta el cierre — 1.63 estaba bien"): la contabilidad de Primas vuelve EXACTAMENTE a la de v1.63. Los credit spreads (IC/Earnings/Broken Wings/Credit Spread) no registran nada en la apertura y materializan el NETO (crédito − recompra) × qty − fee en el evento de cierre; closedResultOf y la tarjeta de Estrategias recuperan noOpenBooked con isCreditSpread; el crédito pendiente de un spread abierto vuelve a mostrarse vía csPending/csCredit. La v1.64 (crédito en la apertura, recompra en el cierre, como un short put) queda descartada: es la convención de Victor para B3 y es coherente — todo el bloque táctico refleja el resultado cuando se realiza, no cuando se cobra. Se conserva TODO lo demás de 1.63/1.64 que no era contabilidad: etiqueta "Crédito definido", categorías Credit Spreads/Broken Wings en Estrategias, % sobre riesgo, comparador de anchos con barra de riesgo, POP suavizada, strikes con % real.

v1.63: (1) EARNINGS o IC son estrategias de CRÉDITO — cobran prima, y la app las anunciaba como "Débito" en la tarjeta de la posición y en el selector Tipo del editor, que es sencillamente falso. Nueva etiqueta "Crédito definido" (natLabelOf/natsForPos): cobra prima, pero el riesgo lo cierran las alas. El id interno sigue siendo DEF A PROPÓSITO (riesgo definido, P&L manual, sin rolls, prima realizada al cierre: cambiarlo movería el riesgo, el BEP y el P&L de todas las posiciones ya guardadas) — lo que cambia es lo que se ve. Además Credit Spread y Broken Wings salen del cajón "Otras" en Primas (donde estaban con los débitos) y ganan categoría propia. (2) % SOBRE RIESGO en todas partes (crédito / pérdida máx): NO es el % del ancho — con ala 2 y $0.60 el % del ancho es 30% pero sobre riesgo es 43%. El % del ancho fija el WR mínimo; el % sobre riesgo es la rentabilidad de la operación. Sale en el panel del ala, en cada fila del comparador, en el badge de calidad y como tile propio en Sizing. (3) MENOS TEXTO, MÁS VISUAL: cada ala del comparador estrena BARRA DE RIESGO de 22px — verde entre los strikes cortos (crédito entero), ámbar la zona de pérdida PARCIAL (corto→largo: donde un ala ancha amortigua y una estrecha ya no), rojo la pérdida máxima, y los movimientos de open REALES de tu histórico como puntos pintados por lo que habrían costado. Esa barra dice de un vistazo lo que antes eran tres párrafos. Fuera los explicativos largos de POP suavizada (ahora "crudo → usada" en una línea), comparador, plan B, lector de tablas y aviso de asimetría (ahora "Flanco débil: PUT ▾ · 4.17% vs 4.87%").

v1.62: (1) COMPARADOR DE ANCHOS — el panel del ala gana su propio campo de crédito y un botón "+" que congela ese ancho en un comparador: eliges ala, apuntas lo que paga el bróker, "+", cambias de ala y repites; abajo se ordenan solos con un veredicto "Ancho recomendado" razonado desde los datos. Cada ancho se juzga con DOS modelos y se queda con el PEOR (prudencia): RÉPLICA (pasa cada movimiento de open real del histórico por el payoff exacto del cóndor — condorValueAt — así una rotura suave cuesta lo que cuesta y no la pérdida máxima entera: es el modelo que VE el colchón del ala ancha) y BINARIO (POP suavizada × crédito − (1−POP) × pérdida máx: pesimista con el ala ancha, pero es el único que deja hueco a la cola que el histórico aún no ha visto). El EV se compara a IGUAL RIESGO OBJETIVO (contratos = riesgo/pérdida máx), que es como se decide de verdad — el % del ancho por sí solo no vale: un ala estrecha SIEMPRE saca mejor ratio porque la curva de precios es convexa, eso es geometría, no edge. Cada fila enseña además dónde toca pérdida máxima en % y cuántos earnings del histórico cayeron limpios / parciales / al máximo. (2) POP SUAVIZADA (Laplace, (M−X+1)/(M+2)): con M=12 y 0 roturas el crudo daba 100% e inflaba el EV y el veredicto justo donde menos datos hay; las 4 tarjetas de POP siguen enseñando el dato crudo (es el hecho) pero EV, veredicto, reco de estrategia y tarjetas del carrusel van sobre la suavizada. (3) STRIKES CON SU % REAL: cada strike enseña el movimiento que implica DESPUÉS del redondeo al incremento del bróker (no el EM nominal), en verde si el redondeo lo dejó fuera del EM y ámbar si lo metió dentro, más un aviso de cóndor asimétrico que dice por qué flanco te van a romper primero.

v1.61 FIX (Victor: "esto no ajusta según el tamaño de ala que marque"): el sizing (pérdida máx/contrato, contratos, WR mínimo, EV, veredicto) lee el ancho de los STRIKES escritos, no del selector, así que cambiar el ala no movía ningún número hasta volver a pulsar "Generar spreads". Ahora el selector regenera los spreads al vuelo (setWing) siempre que haya spot y EM, y el panel del ala muestra el ancho REAL de los strikes (r.width), cayendo al selector solo mientras no haya spreads — el selector y el cálculo ya no pueden discrepar.

v1.60: ScanOverlay — overlay a pantalla completa mientras se lee una captura (wizard, comparador de puts y tabla de earnings): anillo girando con el icono de cámara dentro, título, y la lista de pasos (Preparando la imagen · Enviando al lector · Leyendo los datos · Volcando al formulario) marcando en verde los ya hechos y pulsando el vivo; en los lotes el subtítulo lleva la cuenta ("Captura 2 de 6", "3 de 6 capturas"). Los pasos los marca el propio lector conforme ocurren: no hay progreso inventado. Antes solo cambiaba el texto de un botón y con varias capturas parecía colgado.

v1.59: la tarjeta de movers ya no desaparece cuando aún no hay datos del día (hace falta un 🔄 Precios para traer el cierre anterior de cada subyacente): se pinta igual, con un botón "Actualizar precios" dentro — una acción, no un cartel.

v1.58 (la 1.57 dejó a Victor sin tarjeta: exigía un snapshot del día ANTERIOR con el nuevo formato): los movers ya no dependen de snapshots. Para las posiciones con P&L automático (acciones) el movimiento del día es exacto y directo — se reevalúa el MISMO effPnl con el cierre anterior de Finnhub como spot y se resta (vale igual para acciones, lineales, que para opciones cortas, no lineales), disponible desde el primer 🔄 Precios. Para las estructuras con P&L manual (condors, spreads, 0DTE) la app no tiene mark intradía, así que su Δ sale del snapshot por posición (posPnl) si lo hay y si no es 0 — nunca inventado. Se mantiene la regla clave: solo posiciones que siguen abiertas, para que la rotación no se cuele como movimiento (el fantasma de SPX).

v1.57 FIX (Victor: "si SPX no me genera profit con su movimiento, ¿por qué me sale ahí?"): los movers comparaban el P&L AGREGADO POR TICKER contra el snapshot, así que la rotación de posiciones se colaba como movimiento de mercado — cerrar los 0DTE de SPX y abrir otros el mismo día hacía aparecer una "ganancia" que nadie ganó. Ahora buildSnapshot guarda el P&L por POSICIÓN (posPnl: id → pnl) y los movers solo suman posiciones presentes en ambos momentos (misma id): lo que se ha movido de verdad en lo que sigue abierto. Las posiciones abiertas o cerradas desde el snapshot no cuentan. Los snapshots antiguos no llevan posPnl, así que la tarjeta no se pinta hasta el siguiente 🔄 Precios.

v1.56: las tarjetas de los movers tienen todas la misma altura — la chapa del % se pinta siempre, neutra con "—" cuando el subyacente no trae cotización del día (índices como SPX en el plan gratis de Finnhub), en vez de desaparecer y encoger la tarjeta; el importe se alinea arriba, a la altura del ticker.

v1.55: en los movers, el % del subyacente baja DEBAJO DEL TICKER en su chapa de color (verde/roja), y el importe de tu posición queda solo a la derecha — cada bloque con su dueño: izquierda el subyacente, derecha tu posición.

v1.54: cada mover pasa a ser una tarjeta con fondo — ticker a la izquierda y, a la derecha, el importe grande con el % del subyacente JUSTO DEBAJO, ambos alineados a la derecha y en el mismo color; barra al pie dentro de la tarjeta.

v1.53: en los movers, las cifras se alinean a la DERECHA — el % y el importe comparten borde derecho (el ticker sigue a la izquierda), que es como mejor se leen en columna.

v1.52: fila de los movers — ticker y % del subyacente en la MISMA línea (ticker a la izquierda, % alineado a la derecha, sin chapa) y el importe debajo en grande; en dos columnas estrechas los tres no caben en una fila sin truncar el ticker.

v1.51: fila de los movers reordenada — el ticker se truncaba ("A…", "HI…") porque competía por el ancho con la chapa del % y el importe; ahora el importe va grande arriba y debajo, en pequeño, el TICKER con su % entre paréntesis (verde/rojo).

v1.50: fuera el aviso de texto en los movers (v1.49) — el % del subyacente sigue apareciendo en cuanto se refrescan precios con la v1.48+, sin cartel que lo explique.

v1.48: el % de cambio del SUBYACENTE en los movers pasa a chapa verde/roja junto al ticker, y sale del propio Finnhub (el endpoint /quote ya devuelve dp y pc: no hay que derivarlo de snapshots, así que funciona desde el primer refresco). Se guarda en KEY_DAYPCT con la fecha de la lectura y se ignora si es de otro día — nunca se enseña un % viejo como si fuera de hoy; es dato de mercado re-descargable, así que NO entra en el backup. Si por lo que sea no hay dato de Finnhub, se cae al % derivado del último snapshot.

v1.47: MOVERS DEL DÍA — debajo de la distribución por bloques, dos columnas (Ganadores / Perdedores) con los 3 mayores de cada lado: Δ P&L de la posición desde el cierre del último día con snapshot, Δ% del subyacente al lado (si ese snapshot guardó cotización) y barra proporcional al mayor movimiento del día. buildSnapshot guarda ahora el último precio por ticker y el desglose de tickers por bróker, así que los movers también funcionan con un bróker filtrado. Solo entran tickers que ya existían en el snapshot previo: una posición abierta hoy es capital nuevo, no un movimiento. Sin snapshot previo la tarjeta no se pinta.

v1.46: el cambio del día también funciona por bróker — buildSnapshot guarda ahora byBroker { nlv, mktvl, margin } con las mismas definiciones que el consolidado, y dayChg toma la referencia del bróker filtrado; un snapshot anterior a v1.46 no tiene ese desglose, así que no sirve de referencia para un bróker suelto (se salta en vez de compararlo contra el total) — el Δ por bróker aparece en cuanto guardes un snapshot nuevo con 🔄 Precios.

v1.45: los cuatro tiles del hero se reordenan a Riesgo · MKT VL · Posiciones · Liquidez.

v1.44: los ratios del hero vuelven al formato compacto del estado de cuenta anterior (EL/NLV sobre EL/MRGN apilados con filete + apalancamiento), en la misma fila que el ▼ que despliega el detalle — tocar un ratio abre su ficha, tocar el resto de la fila despliega. Sustituyen a los tres tiles grandes de salud.

v1.43: la tarjeta "Valor de la cuenta" (AccountHero) sale de la pestaña Resumen y pasa a estar FIJA arriba de Portfolio, encima de la fila de pestañas — se sigue viendo mientras deslizas por B0, B1…, con su expandible de estado de cuenta intacto. La pestaña Resumen se queda con la distribución por bloques (BlockDistribution: donut + leyenda con bandas objetivo). El cambio del día pierde el texto: solo importe y % en verde/rojo, medidos desde el cierre del último día con snapshot (la medianoche anterior); sin referencia no se pinta nada.

v1.42: el Resumen ejecutivo ABSORBE el estado de cuenta y la tarjeta oscura de riesgo/P&L (ambos desaparecen de Portfolio; la tarjeta de cuenta sigue en Herramientas): el hero lleva NLV (28px), cambio del día debajo en verde/rojo (importe + % + fecha de referencia), P&L abierto, tiles Pos./Riesgo/MKT VL/Liquidez, salud (EL/NLV, EL/margen, apalancamiento) y un expandible "Estado de cuenta" con los campos de cash negativo y margen por bróker, Excess Liq, avisos y las barras de zona EL/NLV y EL/MRGN. El cambio del día se calcula contra el snapshot más reciente de un día anterior (los guarda 🔄 Precios); sin snapshot previo lo dice en vez de inventar, y con un filtro de bróker activo no se muestra (los snapshots son de la cartera entera).

v1.41: RESUMEN EJECUTIVO — nueva primera pestaña de Portfolio (y la que abre por defecto), de la que se sigue deslizando a B0, B1… con el gesto de siempre. Hero con NLV grande, P&L abierto y tiles de posiciones/riesgo/liquidez + salud de cuenta (EL/NLV, EL/margen, apalancamiento) coloreada por sus umbrales y tocable para abrir su ficha. Debajo, donut de DISTRIBUCIÓN POR BLOQUES (% de NLV que ocupa cada uno) con el % desplegado en el centro; tocar una porción muestra su bloque en el centro y resalta su fila. Leyenda con una fila por bloque: capital, % de NLV en el color de su estado, barra con la BANDA OBJETIVO marcada en el carril y el desvío en texto ("en banda" / "por debajo X" / "por encima X"); tocar el nombre salta a la pestaña de ese bloque.

v1.40 FIX maquetación de la tarjeta combinada (se solapaban/pisaban datos en iOS): items del carrusel con width explícito en vez de flex-basis % (Safari lo resuelve mal contra el padding del scroller); la tarjeta pasa de grid a columna flex con gap y overflow:hidden; TKR y chapa con elipsis/flex:none; cada mini-tile con minWidth:0 + overflow:hidden y valor/etiqueta con elipsis (fuentes 14/13) para que nunca derramen sobre el vecino; el pie gana flexWrap para que los 3 botones bajen de línea en pantallas estrechas en vez de aplastar el texto.

v1.39: (1) el comparador de anchos SUBE junto al selector del ala — al elegir el ancho aparece el crédito mínimo a pedir (⅓) y, con el crédito apuntado, la misma fórmula que vivía abajo: ratio, pérdida/contrato, WR mínimo (break-even), contratos contra el riesgo objetivo y EV con la POP open del histórico; desaparecen la sección "Comparar anchos de ala" y los campos cr2/cr25/cr3/cr5/cr10. (2) UN solo carrusel con tarjeta COMBINADA por ticker (unión de ideas + histórico, orden alfabético): TKR + chapa (GO/CAUTION/NO-GO, A MEDIAS si la idea está incompleta, SOLO TABLA si no hay idea), estructura+EM, barra POP open, fila de mini-tiles de la idea (Crédito/EV/Contratos) y fila del histórico (IV crush/Peor gap/|open|/EM); pie con N, próximo ± y fecha + botones Abrir, ✕ idea y ✕ tabla (dos toques cada uno); tocar carga la idea o, si no hay, vuelca el histórico. El carrusel de abajo desaparece; la sección del histórico queda con el lector de tablas y los campos M/X/U.

v1.38: los dos carruseles comparten ORDEN — alfabético por ticker (ideasSorted / histSyms), así la posición N de arriba y la de abajo son el mismo ticker cuando existe en ambos; el histórico deja de ordenarse por última actualización.

v1.37: sincronía entre los dos carruseles — deslizar el de ideas lleva el del histórico al mismo ticker y viceversa (SnapCarousel gana keys/activeKey/onActiveKey; scroll suave programático con guarda anti-bucle progRef; solo mueve el scroll, no carga nada), y tocar/cargar una tarjeta también alinea el otro carrusel. Además las tarjetas de ideas pasan a stats VIVAS: si el ticker tiene histórico de tablas, M/X/U (y por tanto POP/EV/veredicto) salen del histórico actual en vez de los valores congelados al guardar — releer la tabla tras un earnings actualiza la idea sola; cargar la idea también carga los vivos.

v1.36 FIX: los carruseles se salían del ancho del móvil — SnapCarousel es hijo directo de un grid y los items de grid tienen min-width:auto por defecto, así que el track crecía al max-content de todas las tarjetas en fila; minWidth:0/maxWidth:100% en el carrusel y minWidth:0 en los wrappers y tarjetas, y fuera el nowrap del mini-tile |open|/EM.

v1.35 UX: carrusel deslizable con snap (SnapCarousel: scroll horizontal nativo + scroll-snap, momentum de iOS, dots que siguen la posición, sangrado a los bordes de la tarjeta; con una sola tarjeta no hay scroll ni dots) para las ideas guardadas y el histórico de tablas; tarjetas rediseñadas — idea: TKR grande + chapa GO/CAUTION/NO-GO (o A MEDIAS), estructura y EM, barra de POP open del color del veredicto y mini-tiles Crédito/EV/Contratos; histórico: TKR + N + próximo ±, barra de POP open y mini-tiles IV crush / Peor gap / |open|/EM. Los botones Abrir y ✕ viven en el pie de cada tarjeta. No choca con el swipe entre bloques (ignora gestos nacidos en scroll horizontal).

v1.34: (1) lector de tablas MULTI-captura — hasta 6 de golpe, una tabla por ticker, cada una guardada bajo el ticker de SU cabecera (en lote, sin cabecera se salta con aviso; el campo Ticker solo hace de respaldo con una única foto); la calculadora se rellena con el ticker del campo si está en el lote, o con el primero. (2) Botón Limpiar junto a Guardar idea: vacía solo el formulario (riesgo objetivo se conserva; ideas e histórico intactos, tienen su ✕ propia).

v1.33: anchos de ala de 2 y 3 puntos (Victor: a veces el strike no cuadra con patas de 2.5) — se añaden al selector "Ancho del ala" del generador de spreads y como filas propias del comparador de anchos (campos cr2/cr3, persistidos en el formulario e ideas como el resto).

v1.32: ideas de earnings guardadas (KEY_ERIDEAS, entra en el backup) — como las tarjetas del Comparador: "Guardar idea · TKR" congela la foto completa del formulario (una por ticker, reguardar actualiza); lista de tarjetas arriba de la calculadora con veredicto GO/CAUTION/NO-GO, estructura, crédito, POP open, EV y contratos (erIdeaDerive, misma matemática que la calculadora); tocar la tarjeta la carga (con el riesgo objetivo global vigente, no el congelado), botón Abrir salta al wizard prefijado y borrar con dos toques. El flujo: preparar las ideas el día de antes y el día del trade cargar, refrescar el crédito y abrir.

v1.31: wording — se retiran las referencias a moomoo (botón, textos y comentarios); el lector es de la tabla Historical Earnings Data, venga del bróker que venga.

v1.30: el lector de la tabla extrae el TICKER de la propia captura si incluye la cabecera (arriba a la izquierda) — ya no hace falta teclearlo antes; si el campo y la captura no coinciden, manda la captura (es la fuente de los datos) y se avisa. Se elimina el lector de gráficas de 4 capturas (extractEarningsChartFromImage, tallyEarningsChart, runChartOcr y su UI): la tabla da lo mismo y más con una sola foto.

v1.29: histórico persistente de earnings por ticker (KEY_ERHIST, entra en el backup) alimentado por un lector de la tabla "Historical Earnings Data" de moomoo — UNA captura trae EM, IV crush y movimientos antes/durante/después por trimestre (la gráfica de 4 capturas queda como plan B). Requiere el ticker escrito antes de leer (la tabla no lo muestra); merge por periodo al releer (el próximo ER pasa de solo-EM a fila completa cuando salen resultados); tarjetas por ticker en la herramienta Earnings (POP open, N, IV crush medio, peor gap = el open de mayor magnitud, |open| medio vs EM medio, próximo ±) — tocar la tarjeta vuelca M/X/U a la calculadora y el EM pendiente al campo Expected move; borrar con dos toques.

v1.28 FIX: el cierre de un Long Call registraba en Primas la venta bruta entera (closePrice×qty) en vez del neto (venta − coste), inflando Primas con un débito que nunca se restó; y un Long Call expirado sin valor no registraba la pérdida. Ahora el Long Call materializa el NETO al cierre, igual que los débitos genéricos — coherente con su apertura, que (correctamente) no registra nada. MTM ya era correcto.

v1.27: se limpian los dos últimos emojis (botón de leer capturas del Comparador y texto de Ajustes)

v1.26: Comparador de puts — icono propio en el selector, ticker forzado a mayúsculas y botón de spot (Finnhub) junto al precio del subyacente, igual que en la herramienta de earnings

v1.25: "Abrir posición" desde las Herramientas (Earnings → B3/Earnings, Comparador de puts → B1/B2 Short Put) que salta al wizard con todo relleno; selector de Herramientas con iconos igual que Movimientos; etiquetas del histórico acortadas; iconos de línea (ActIcon) en vez de emojis en editar/rolar/lotes/dividendos

v1.24: wording — se quitan las referencias al PDF en la UI; nota de High/Low aclarada (extremos del recorrido intradía medidos contra el cierre previo)

v1.23: panel "Aguanta o cierra" (plan B leído de las 4 series: reversión intradía close vs open y excursión adversa high/low) + comparador de anchos de ala (crédito por ancho → riesgo/contrato, ratio ⅓, contratos y EV lado a lado)

v1.22: lector de gráficas Expected vs Actual — sube 1-4 capturas, Gemini detecta la serie por la leyenda (Open/Close/High/Low), extrae expected/actual por trimestre y rellena M, X por escenario y U (cierres en verde)

v1.21: pestaña Herramientas (selector Puts | Earnings), CondorRiskInfo fuera del wizard (sigue en el editor), y herramienta Earnings completa: spot vía Finnhub, generador de spreads desde expected move, POP por escenario (open/close/high/low), sesgo direccional con recomendación de estrategia (PDF), sizing y EV

v1.19: riesgo de cóndores (Earnings/IC): pérdida máx auto desde las alas, contratos sugeridos vs riesgo objetivo, badge crédito/ancho en wizard+editor, y Calculadora Earnings en el Comparador (sizing + POP histórica + EV)
